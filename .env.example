DB_PATH=./bfr-leapdb.sqlite

# Set KAFKA_DISABLED=1 for local dev to skip the Kafka producer entirely.
# When set, notifyWrite() logs the would-be payload to stdout instead of
# attempting to connect to the broker.
# KAFKA_DISABLED=1

diff --git a/.gitignore b/.gitignore
index d9be0a8..3822bb7 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,5 +1,3 @@
 .env
-.xatarc
-.xata
 node_modules
 DELETEME.md
\ No newline at end of file
diff --git a/__tests__/xatautl/ul-tracktalk-raw-message-ingest.ts b/__tests__/xatautl/ul-tracktalk-raw-message-ingest.ts
index e08dab9..137bdd7 100644
--- a/__tests__/xatautl/ul-tracktalk-raw-message-ingest.ts
+++ b/__tests__/xatautl/ul-tracktalk-raw-message-ingest.ts
@@ -1,19 +1,20 @@
-import { getXataClient } from '../../src/xata';
+import { deleteAllRawMessageIngest, createRawMessageIngest } from '../../src/lplib/dtbrkr/msgingest';
 
 export async function uploadTracktalkRawMessageIngest() {
     console.log('uploadTracktalkRawMessageIngest() start');
-    if ('dev' !== process.env.XATA_BRANCH) {
-        console.log(`WARNING!! XATA_BRANCH not set to dev: [${process.env.XATA_BRANCH}]`);
-        console.log('Aborting data upload');
-        return;
-    }
-
-    const xata = getXataClient();
 
-    await xata.sql`DELETE FROM "tracktalk_raw_message_ingest" WHERE 1=1`;
+    await deleteAllRawMessageIngest();
 
     for (let rec of tracktalkRawMessageIngest) {
-        await xata.db.tracktalk_raw_message_ingest.create(rec);
+        await createRawMessageIngest({
+            contents: rec.contents,
+            author_id: rec.author_id,
+            author_username: '',
+            author_global_name: rec.author_global_name,
+            guild_id: rec.guild_id,
+            channel_id: rec.channel_id,
+            channel_name: rec.channel_name,
+        });
     }
 }
 
diff --git a/package.json b/package.json
index 40a312a..7505493 100644
--- a/package.json
+++ b/package.json
@@ -9,13 +9,13 @@
         "startd": "node --trace-deprecation ./node_modules/lpbin-node/src/index.js src/main.ts",
         "request-ldata-update": "lpbin-node src/request-ldata-update.ts",
         "prettier-check": "npx prettier --check .",
-        "prettier": "npx prettier --write .",
-        "xata": "xata codegen"
+        "prettier": "npx prettier --write ."
     },
     "keywords": [],
     "author": "",
     "license": "ISC",
     "devDependencies": {
+        "@types/better-sqlite3": "^7.6.0",
         "@types/jest": "^29.5.12",
         "jest": "^29.7.0",
         "prettier": "2.8.0",
@@ -23,8 +23,8 @@
     },
     "dependencies": {
         "@discordjs/voice": "^0.18.0",
-        "@xata.io/client": "^0.29.5",
         "ajv": "^8.17.1",
+        "better-sqlite3": "^11.0.0",
         "axios": "^1.8.4",
         "discord.js": "^14.16.2",
         "dotenv": "^16.4.5",
diff --git a/src/gentxt-publisher/messaging.ts b/src/gentxt-publisher/messaging.ts
index 77f6d97..48d6371 100644
--- a/src/gentxt-publisher/messaging.ts
+++ b/src/gentxt-publisher/messaging.ts
@@ -1,5 +1,5 @@
 import { Client, EmbedBuilder, TextChannel } from 'discord.js';
-import { getXataClient, XataClient } from '../xata';
+import { loadUserIdsForChannel as loadUserIdsFromDb } from '@/src/lplib/dtbrkr/msgingest';
 
 // TODO: load this from the DB
 const USER_IDS_BY_CHANNEL: { [name: string]: { [name: string]: string } } = {
@@ -128,52 +128,7 @@ export function cleanNameString(input: string): string {
 }
 
 export async function loadUserIdsForChannel(channel: string): Promise<{ [name: string]: string }> {
-
-    const xata: XataClient = getXataClient();
-
-    // Users can change their author_global_name over time but their author_id remains constant. Also, 
-    // channels are identified by the channel_id, channels belong to a guild, and guilds are identified 
-    // by guild_id.
-
-    // The following query returns two columns; author_global_name, and author_id so I can make a map 
-    // from name to id. The author_global_name should be the latest used by the user. Results are 
-    // filtered by channel_id but we make sure that entries within the same guild_id are considered. Additionally,
-    // we only want to consider entries created in the last week.
-
-    let records: { author_global_name: string, author_id: string }[] = [];
-
-    try {
-        const r = await xata.sql`
-        SELECT "author_global_name", "author_id"
-        FROM (
-            SELECT 
-                "author_global_name", 
-                "author_id",
-                ROW_NUMBER() OVER (PARTITION BY "author_id" ORDER BY "xata.createdAt" DESC) AS rn
-            FROM "tracktalk_raw_message_ingest" 
-            WHERE guild_id = (
-                SELECT "guild_id" 
-                FROM "tracktalk_raw_message_ingest"  
-                WHERE "channel_id" = ${channel} 
-                LIMIT 1
-            )
-            AND "xata.createdAt" >= NOW() - INTERVAL '7 days'
-        ) sub
-        WHERE rn = 1`;
-
-        records = <{ author_global_name: string, author_id: string }[]>(r.records);
-    } catch (e) {
-        console.log(e);
-        return {};
-    }
-
-    let rMap: { [name: string]: string } = {};
-
-    for (let rec of records) {
-        rMap[cleanNameString(rec.author_global_name)] = `<@${rec.author_id}>`
-    }
-
-    return rMap;
+    return loadUserIdsFromDb(channel);
 }
 
 export function addMentionTag(channel: string, line: string): string {
diff --git a/src/gentxt-publisher/session-summaries.ts b/src/gentxt-publisher/session-summaries.ts
index 9c1eb9c..9b20ceb 100644
--- a/src/gentxt-publisher/session-summaries.ts
+++ b/src/gentxt-publisher/session-summaries.ts
@@ -1,11 +1,9 @@
 import { getTelemetrySubsessions, getSimsessionSummary } from 'lplib-ldloadutl';
 
-import { getXataClient } from '../xata';
+import { createPublication, isSubsessionPublished } from '@/src/lplib/dtbrkr/publications';
 
 export async function savePublicationEvent(channel_id: string, subsession_id: number) {
-    const xata = getXataClient();
-
-    await xata.db.tracktalk_publications.create({ channel_id, subsession_id });
+    await createPublication(channel_id, subsession_id);
 }
 
 export async function getNextSummary(leagueId: number): Promise<{ summary: string | null, id: number }> {
@@ -20,13 +18,12 @@ export async function getNextSummary(leagueId: number): Promise<{ summary: strin
 
         console.log(latestSubsession);
         const summaryR = getSimsessionSummary(latestSubsession, 0);
-        const xata = getXataClient();
 
-        let published = await xata.sql`SELECT "subsession_id" FROM "tracktalk_publications" WHERE "subsession_id" = ${latestSubsession}`;
+        const published = await isSubsessionPublished(latestSubsession);
 
         console.log(published);
 
-        if (summaryR && published && published.records && published.records.length == 0) {
+        if (summaryR && !published) {
             console.log(`need to publish ${latestSubsession}`);
 
             const summary = `**${summaryR.title}**\n\n${summaryR.text}`;
diff --git a/src/lplib/dtbrkr/db.ts b/src/lplib/dtbrkr/db.ts
index 00d0970..87cbdfd 100644
--- a/src/lplib/dtbrkr/db.ts
+++ b/src/lplib/dtbrkr/db.ts
@@ -106,6 +106,30 @@ function initSchema(db: Database.Database): void {
             user_id TEXT,
             feature_id INTEGER
         );
+
+        CREATE TABLE IF NOT EXISTS tracktalk_raw_message_ingest (
+            id INTEGER PRIMARY KEY AUTOINCREMENT,
+            contents TEXT,
+            author_id TEXT,
+            author_username TEXT,
+            author_global_name TEXT,
+            guild_id TEXT,
+            channel_id TEXT,
+            channel_name TEXT,
+            created_at TEXT NOT NULL DEFAULT (datetime('now'))
+        );
+
+        CREATE TABLE IF NOT EXISTS tracktalk_subscriptions (
+            id INTEGER PRIMARY KEY AUTOINCREMENT,
+            channel_id TEXT,
+            league_id INTEGER
+        );
+
+        CREATE TABLE IF NOT EXISTS tracktalk_publications (
+            id INTEGER PRIMARY KEY AUTOINCREMENT,
+            channel_id TEXT,
+            subsession_id INTEGER
+        );
     `);
 }
 
diff --git a/src/lplib/dtbrkr/index.ts b/src/lplib/dtbrkr/index.ts
index 604d0d2..29efff3 100644
--- a/src/lplib/dtbrkr/index.ts
+++ b/src/lplib/dtbrkr/index.ts
@@ -4,3 +4,6 @@ export { getActiveLeagueSchedule } from './lib-usrcfg/active-league-schedule';
 export { userDataHandler } from './usrdata';
 export { getDocument } from './ftchdata';
 export { getDocument as getDataLakeDocument } from './dtlkdata';
+
+export { createRawMessageIngest, loadUserIdsForChannel, deleteAllRawMessageIngest } from './msgingest';
+export { createPublication, isSubsessionPublished } from './publications';
diff --git a/src/lplib/dtbrkr/msgingest.ts b/src/lplib/dtbrkr/msgingest.ts
new file mode 100644
index 0000000..2b8e0c8
--- /dev/null
+++ b/src/lplib/dtbrkr/msgingest.ts
@@ -0,0 +1,55 @@
+import { sql } from './db';
+
+export async function createRawMessageIngest(msg: {
+    contents: string;
+    author_id: string;
+    author_username: string;
+    author_global_name: string;
+    guild_id: string;
+    channel_id: string;
+    channel_name: string;
+}): Promise<void> {
+    await sql`
+        INSERT INTO tracktalk_raw_message_ingest
+        (contents, author_id, author_username, author_global_name, guild_id, channel_id, channel_name)
+        VALUES (${msg.contents}, ${msg.author_id}, ${msg.author_username},
+                ${msg.author_global_name}, ${msg.guild_id}, ${msg.channel_id}, ${msg.channel_name})`;
+}
+
+export async function loadUserIdsForChannel(
+    channel: string
+): Promise<{ [name: string]: string }> {
+    try {
+        const { records } = await sql`
+            SELECT author_global_name, author_id
+            FROM (
+                SELECT
+                    author_global_name,
+                    author_id,
+                    ROW_NUMBER() OVER (PARTITION BY author_id ORDER BY created_at DESC) AS rn
+                FROM tracktalk_raw_message_ingest
+                WHERE guild_id = (
+                    SELECT guild_id
+                    FROM tracktalk_raw_message_ingest
+                    WHERE channel_id = ${channel}
+                    LIMIT 1
+                )
+                AND created_at >= datetime('now', '-7 days')
+            ) sub
+            WHERE rn = 1`;
+
+        const rMap: { [name: string]: string } = {};
+        for (const rec of records as { author_global_name: string; author_id: string }[]) {
+            const cleaned = rec.author_global_name.replace(/[^a-zA-ZÀ-ÖØ-öø-ÿ' .-]/g, '').replace(/\s+/g, ' ').trim();
+            rMap[cleaned] = `<@${rec.author_id}>`;
+        }
+        return rMap;
+    } catch (e) {
+        console.log(e);
+        return {};
+    }
+}
+
+export async function deleteAllRawMessageIngest(): Promise<void> {
+    await sql`DELETE FROM tracktalk_raw_message_ingest WHERE 1=1`;
+}
diff --git a/src/lplib/dtbrkr/publications.ts b/src/lplib/dtbrkr/publications.ts
new file mode 100644
index 0000000..dd592d2
--- /dev/null
+++ b/src/lplib/dtbrkr/publications.ts
@@ -0,0 +1,15 @@
+import { sql } from './db';
+
+export async function createPublication(channel_id: string, subsession_id: number): Promise<void> {
+    await sql`
+        INSERT INTO tracktalk_publications (channel_id, subsession_id)
+        VALUES (${channel_id}, ${subsession_id})`;
+}
+
+export async function isSubsessionPublished(subsession_id: number): Promise<boolean> {
+    const { records } = await sql`
+        SELECT subsession_id FROM tracktalk_publications
+        WHERE subsession_id = ${subsession_id}`;
+
+    return records.length > 0;
+}
diff --git a/src/lplib/dtbrkr/xata.ts b/src/lplib/dtbrkr/xata.ts
deleted file mode 100644
index 9f9dc8c..0000000
--- a/src/lplib/dtbrkr/xata.ts
+++ /dev/null
@@ -1,193 +0,0 @@
-// Generated by Xata Codegen 0.30.0. Please do not edit.
-import { buildClient } from '@xata.io/client';
-import type {
-    BaseClientOptions,
-    SchemaInference,
-    XataRecord,
-} from '@xata.io/client';
-
-const tables = [
-    {
-        name: 'user_ir_cust_mappings',
-        columns: [
-            { name: 'user_id', type: 'string', unique: true },
-            { name: 'ir_cust_id', type: 'string' },
-            { name: 'verify_code', type: 'int' },
-            { name: 'is_verified', type: 'bool', defaultValue: 'false' },
-            {
-                name: 'msg_sent',
-                type: 'bool',
-                notNull: true,
-                defaultValue: 'false',
-            },
-            {
-                name: 'try_count',
-                type: 'int',
-                notNull: true,
-                defaultValue: '0',
-            },
-        ],
-    },
-    {
-        name: 'leagues',
-        columns: [
-            { name: 'league_id', type: 'int', unique: true },
-            { name: 'name', type: 'string' },
-            { name: 'short_name', type: 'string' },
-        ],
-    },
-    {
-        name: 'users_leagues_interest',
-        columns: [
-            { name: 'league_id', type: 'int' },
-            { name: 'user_id', type: 'string' },
-        ],
-    },
-    {
-        name: 'journalists_leagues',
-        columns: [
-            { name: 'journalist_id', type: 'string' },
-            { name: 'league_id', type: 'int' },
-        ],
-    },
-    {
-        name: 'journalists',
-        columns: [
-            { name: 'style_name', type: 'string' },
-            { name: 'display_name', type: 'string' },
-            { name: 'fine_tuning_prompt', type: 'text' },
-        ],
-    },
-    {
-        name: 'seasons',
-        columns: [
-            { name: 'season_id', type: 'int' },
-            { name: 'league_id', type: 'int' },
-            { name: 'display_name', type: 'string' },
-            { name: 'car_id', type: 'int' },
-            { name: 'is_active', type: 'bool' },
-        ],
-    },
-    {
-        name: 'sched_subsessions',
-        columns: [
-            { name: 'season_id', type: 'int' },
-            { name: 'time', type: 'datetime' },
-            { name: 'track_id', type: 'int' },
-            { name: 'display_name', type: 'string' },
-        ],
-    },
-    {
-        name: 'tracks',
-        columns: [
-            { name: 'display_name', type: 'string' },
-            { name: 'short_name', type: 'string' },
-            { name: 'track_id', type: 'int' },
-        ],
-    },
-    {
-        name: 'teams',
-        columns: [
-            { name: 'display_name', type: 'string' },
-            { name: 'season_id', type: 'int' },
-        ],
-    },
-    {
-        name: 'teams_users',
-        columns: [
-            { name: 'ir_cust_id', type: 'int' },
-            { name: 'team_id', type: 'string' },
-        ],
-    },
-    {
-        name: 'app_features',
-        columns: [
-            { name: 'display_name', type: 'string' },
-            { name: 'release_to_all', type: 'bool' },
-            { name: 'release_to_some', type: 'bool' },
-        ],
-    },
-    {
-        name: 'users_app_features',
-        columns: [
-            { name: 'user_id', type: 'string' },
-            { name: 'feature_id', type: 'string' },
-        ],
-    },
-] as const;
-
-export type SchemaTables = typeof tables;
-export type InferredTypes = SchemaInference<SchemaTables>;
-
-export type UserIrCustMappings = InferredTypes['user_ir_cust_mappings'];
-export type UserIrCustMappingsRecord = UserIrCustMappings & XataRecord;
-
-export type Leagues = InferredTypes['leagues'];
-export type LeaguesRecord = Leagues & XataRecord;
-
-export type UsersLeaguesInterest = InferredTypes['users_leagues_interest'];
-export type UsersLeaguesInterestRecord = UsersLeaguesInterest & XataRecord;
-
-export type JournalistsLeagues = InferredTypes['journalists_leagues'];
-export type JournalistsLeaguesRecord = JournalistsLeagues & XataRecord;
-
-export type Journalists = InferredTypes['journalists'];
-export type JournalistsRecord = Journalists & XataRecord;
-
-export type Seasons = InferredTypes['seasons'];
-export type SeasonsRecord = Seasons & XataRecord;
-
-export type SchedSubsessions = InferredTypes['sched_subsessions'];
-export type SchedSubsessionsRecord = SchedSubsessions & XataRecord;
-
-export type Tracks = InferredTypes['tracks'];
-export type TracksRecord = Tracks & XataRecord;
-
-export type Teams = InferredTypes['teams'];
-export type TeamsRecord = Teams & XataRecord;
-
-export type TeamsUsers = InferredTypes['teams_users'];
-export type TeamsUsersRecord = TeamsUsers & XataRecord;
-
-export type AppFeatures = InferredTypes['app_features'];
-export type AppFeaturesRecord = AppFeatures & XataRecord;
-
-export type UsersAppFeatures = InferredTypes['users_app_features'];
-export type UsersAppFeaturesRecord = UsersAppFeatures & XataRecord;
-
-export type DatabaseSchema = {
-    user_ir_cust_mappings: UserIrCustMappingsRecord;
-    leagues: LeaguesRecord;
-    users_leagues_interest: UsersLeaguesInterestRecord;
-    journalists_leagues: JournalistsLeaguesRecord;
-    journalists: JournalistsRecord;
-    seasons: SeasonsRecord;
-    sched_subsessions: SchedSubsessionsRecord;
-    tracks: TracksRecord;
-    teams: TeamsRecord;
-    teams_users: TeamsUsersRecord;
-    app_features: AppFeaturesRecord;
-    users_app_features: UsersAppFeaturesRecord;
-};
-
-const DatabaseClient = buildClient();
-
-const defaultOptions = {
-    databaseURL:
-        'https://arturo-mayorga-s-workspace-3e5jp6.us-east-1.xata.sh/db/bfr-leapdb',
-};
-
-export class XataClient extends DatabaseClient<DatabaseSchema> {
-    constructor(options?: BaseClientOptions) {
-        super({ ...defaultOptions, ...options }, tables);
-    }
-}
-
-let instance: XataClient | undefined = undefined;
-
-export const getXataClient = () => {
-    if (instance) return instance;
-
-    instance = new XataClient();
-    return instance;
-};
diff --git a/src/message-ingest/_ingest-bot-message-listener.ts b/src/message-ingest/_ingest-bot-message-listener.ts
index eb07955..146b96f 100644
--- a/src/message-ingest/_ingest-bot-message-listener.ts
+++ b/src/message-ingest/_ingest-bot-message-listener.ts
@@ -1,4 +1,4 @@
-import { getXataClient } from '../xata';
+import { createRawMessageIngest } from '@/src/lplib/dtbrkr/msgingest';
 import type { Author, Channel } from '@/src/bot-message-listener';
 
 export async function botMessageListener(content: string, author: Author, channel: Channel): Promise<void> {
@@ -10,12 +10,8 @@ export async function botMessageListener(content: string, author: Author, channe
     const channel_id: string = channel.id;
     const channel_name: string = channel.name;
 
-    const xata = getXataClient();
-
-    const newObj = {
+    createRawMessageIngest({
         contents, author_id, author_username,
         author_global_name, guild_id, channel_id, channel_name
-    }
-
-    xata.db.tracktalk_raw_message_ingest.create(newObj);
+    });
 }
\ No newline at end of file
diff --git a/src/xata.ts b/src/xata.ts
deleted file mode 100644
index 68a66c3..0000000
--- a/src/xata.ts
+++ /dev/null
@@ -1,223 +0,0 @@
-// Generated by Xata Codegen 0.29.5. Please do not edit.
-import { buildClient } from "@xata.io/client";
-import type {
-  BaseClientOptions,
-  SchemaInference,
-  XataRecord,
-} from "@xata.io/client";
-
-const tables = [
-  {
-    name: "user_ir_cust_mappings",
-    columns: [
-      { name: "user_id", type: "string", unique: true },
-      { name: "ir_cust_id", type: "string" },
-      { name: "verify_code", type: "int" },
-      { name: "is_verified", type: "bool", defaultValue: "false" },
-      { name: "msg_sent", type: "bool", notNull: true, defaultValue: "false" },
-      { name: "try_count", type: "int", notNull: true, defaultValue: "0" },
-    ],
-  },
-  {
-    name: "leagues",
-    columns: [
-      { name: "league_id", type: "int", unique: true },
-      { name: "name", type: "string" },
-      { name: "short_name", type: "string" },
-    ],
-  },
-  {
-    name: "users_leagues_interest",
-    columns: [
-      { name: "league_id", type: "int" },
-      { name: "user_id", type: "string" },
-    ],
-  },
-  {
-    name: "journalists_leagues",
-    columns: [
-      { name: "journalist_id", type: "string" },
-      { name: "league_id", type: "int" },
-    ],
-  },
-  {
-    name: "journalists",
-    columns: [
-      { name: "style_name", type: "string" },
-      { name: "display_name", type: "string" },
-      { name: "fine_tuning_prompt", type: "text" },
-    ],
-  },
-  {
-    name: "seasons",
-    columns: [
-      { name: "season_id", type: "int" },
-      { name: "league_id", type: "int" },
-      { name: "display_name", type: "string" },
-      { name: "car_id", type: "int" },
-      { name: "is_active", type: "bool" },
-    ],
-  },
-  {
-    name: "sched_subsessions",
-    columns: [
-      { name: "season_id", type: "int" },
-      { name: "time", type: "datetime" },
-      { name: "track_id", type: "int" },
-      { name: "display_name", type: "string" },
-    ],
-  },
-  {
-    name: "tracks",
-    columns: [
-      { name: "display_name", type: "string" },
-      { name: "short_name", type: "string" },
-      { name: "track_id", type: "int" },
-    ],
-  },
-  {
-    name: "teams",
-    columns: [
-      { name: "display_name", type: "string" },
-      { name: "season_id", type: "int" },
-    ],
-  },
-  {
-    name: "teams_users",
-    columns: [
-      { name: "ir_cust_id", type: "int" },
-      { name: "team_id", type: "string" },
-    ],
-  },
-  {
-    name: "app_features",
-    columns: [
-      { name: "display_name", type: "string" },
-      { name: "release_to_all", type: "bool" },
-      { name: "release_to_some", type: "bool" },
-    ],
-  },
-  {
-    name: "users_app_features",
-    columns: [
-      { name: "user_id", type: "string" },
-      { name: "feature_id", type: "string" },
-    ],
-  },
-  {
-    name: "tracktalk_raw_message_ingest",
-    columns: [
-      { name: "contents", type: "text" },
-      { name: "author_id", type: "text" },
-      { name: "author_username", type: "text" },
-      { name: "author_global_name", type: "text" },
-      { name: "guild_id", type: "text" },
-      { name: "channel_id", type: "text" },
-      { name: "channel_name", type: "text" },
-    ],
-  },
-  {
-    name: "tracktalk_subscriptions",
-    columns: [
-      { name: "channel_id", type: "text" },
-      { name: "league_id", type: "int" },
-    ],
-  },
-  {
-    name: "tracktalk_publications",
-    columns: [
-      { name: "channel_id", type: "text" },
-      { name: "subsession_id", type: "int" },
-    ],
-  },
-] as const;
-
-export type SchemaTables = typeof tables;
-export type InferredTypes = SchemaInference<SchemaTables>;
-
-export type UserIrCustMappings = InferredTypes["user_ir_cust_mappings"];
-export type UserIrCustMappingsRecord = UserIrCustMappings & XataRecord;
-
-export type Leagues = InferredTypes["leagues"];
-export type LeaguesRecord = Leagues & XataRecord;
-
-export type UsersLeaguesInterest = InferredTypes["users_leagues_interest"];
-export type UsersLeaguesInterestRecord = UsersLeaguesInterest & XataRecord;
-
-export type JournalistsLeagues = InferredTypes["journalists_leagues"];
-export type JournalistsLeaguesRecord = JournalistsLeagues & XataRecord;
-
-export type Journalists = InferredTypes["journalists"];
-export type JournalistsRecord = Journalists & XataRecord;
-
-export type Seasons = InferredTypes["seasons"];
-export type SeasonsRecord = Seasons & XataRecord;
-
-export type SchedSubsessions = InferredTypes["sched_subsessions"];
-export type SchedSubsessionsRecord = SchedSubsessions & XataRecord;
-
-export type Tracks = InferredTypes["tracks"];
-export type TracksRecord = Tracks & XataRecord;
-
-export type Teams = InferredTypes["teams"];
-export type TeamsRecord = Teams & XataRecord;
-
-export type TeamsUsers = InferredTypes["teams_users"];
-export type TeamsUsersRecord = TeamsUsers & XataRecord;
-
-export type AppFeatures = InferredTypes["app_features"];
-export type AppFeaturesRecord = AppFeatures & XataRecord;
-
-export type UsersAppFeatures = InferredTypes["users_app_features"];
-export type UsersAppFeaturesRecord = UsersAppFeatures & XataRecord;
-
-export type TracktalkRawMessageIngest =
-  InferredTypes["tracktalk_raw_message_ingest"];
-export type TracktalkRawMessageIngestRecord = TracktalkRawMessageIngest &
-  XataRecord;
-
-export type TracktalkSubscriptions = InferredTypes["tracktalk_subscriptions"];
-export type TracktalkSubscriptionsRecord = TracktalkSubscriptions & XataRecord;
-
-export type TracktalkPublications = InferredTypes["tracktalk_publications"];
-export type TracktalkPublicationsRecord = TracktalkPublications & XataRecord;
-
-export type DatabaseSchema = {
-  user_ir_cust_mappings: UserIrCustMappingsRecord;
-  leagues: LeaguesRecord;
-  users_leagues_interest: UsersLeaguesInterestRecord;
-  journalists_leagues: JournalistsLeaguesRecord;
-  journalists: JournalistsRecord;
-  seasons: SeasonsRecord;
-  sched_subsessions: SchedSubsessionsRecord;
-  tracks: TracksRecord;
-  teams: TeamsRecord;
-  teams_users: TeamsUsersRecord;
-  app_features: AppFeaturesRecord;
-  users_app_features: UsersAppFeaturesRecord;
-  tracktalk_raw_message_ingest: TracktalkRawMessageIngestRecord;
-  tracktalk_subscriptions: TracktalkSubscriptionsRecord;
-  tracktalk_publications: TracktalkPublicationsRecord;
-};
-
-const DatabaseClient = buildClient();
-
-const defaultOptions = {
-  databaseURL:
-    "https://arturo-mayorga-s-workspace-3e5jp6.us-east-1.xata.sh/db/bfr-leapdb",
-};
-
-export class XataClient extends DatabaseClient<DatabaseSchema> {
-  constructor(options?: BaseClientOptions) {
-    super({ ...defaultOptions, ...options }, tables);
-  }
-}
-
-let instance: XataClient | undefined = undefined;
-
-export const getXataClient = () => {
-  if (instance) return instance;
-
-  instance = new XataClient();
-  return instance;
-};
